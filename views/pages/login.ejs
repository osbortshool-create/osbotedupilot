<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
     <link rel="icon" type="image/png" sizes="32x32" href="/images/favicon.png">
</head>
<body class="login-page">
    <div class="login-container">
        <div class="login-header">
            <div class="login-logo">
                <i class="fas fa-graduation-cap"></i>
            </div>
            <h1>EduPilot NG</h1>
            <p>Access your school portal</p>
        </div>
        
        <% if (locals.error) { %>
            <div class="alert alert-error">
                <i class="fas fa-exclamation-triangle"></i>
                <%= error %>
            </div>
        <% } %>
        
        <form class="login-form" method="POST" action="/login">
            <div class="form-group">
                <label for="campus">Select Campus:</label>
                <select name="campus" id="campus" required>
                    <option value="">Select Campus</option>
                    <option value="Lagos">Lagos Campus</option>
                    <option value="Ekiti">Ekiti Campus</option>
                </select>
                <i class="fas fa-school input-icon"></i>
            </div>
            
            <div class="form-group">
                <label for="loginType">Login As:</label>
                <select name="loginType" id="loginType" required>
                    <option value="staff">Staff (Admin/Teacher/Officer)</option>
                    <option value="student">Student</option>
                </select>
                <i class="fas fa-user-tag input-icon"></i>
            </div>
            
            <div class="form-group">
                <label for="email" id="emailLabel">Email Address</label>
                <input type="text" id="email" name="email" required placeholder="Enter your email address">
                <i class="fas fa-envelope input-icon"></i>
            </div>
            
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" required placeholder="Enter your password">
                <i class="fas fa-lock input-icon"></i>
            </div>
            
            <button type="submit" class="btn login-btn">
                <i class="fas fa-sign-in-alt"></i>
                Login to Portal
            </button>
        </form>
        
        <div class="login-help">
            <p>Having trouble logging in? <a href="#contact">Contact the school administration</a></p>
        </div>
    </div>

    <script>
        document.getElementById('loginType').addEventListener('change', function() {
            const emailLabel = document.getElementById('emailLabel');
            const emailInput = document.getElementById('email');
            const emailIcon = document.querySelector('.form-group .input-icon');
            
            if (this.value === 'student') {
                emailLabel.textContent = 'Parent Phone or Parent Email';
                emailInput.placeholder = 'Enter parent phone or email';
                emailIcon.className = 'fas fa-phone input-icon';
            } else {
                emailLabel.textContent = 'Email Address';
                emailInput.placeholder = 'Enter your email address';
                emailIcon.className = 'fas fa-envelope input-icon';
            }
        });
    </script>
</body>
</html>